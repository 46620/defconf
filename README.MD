# Default Configs

This repo is for holding default configs for installs and packages, in case you need a clean version. Each distro should have a `version` file that says what version of the OS it was pulled from.